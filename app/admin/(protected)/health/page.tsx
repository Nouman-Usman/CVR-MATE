import { AdminHealthDashboard } from "@/components/admin/AdminHealthDashboard";

export const metadata = { title: "Operational Health — CVR-MATE Admin" };

export default function AdminHealthPage() {
  return <AdminHealthDashboard />;
}
